use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize)]
pub struct Inventory {
    hearts: u8,
    swords: u8,
    gold_keys: u8,
    silver_keys: u8,
    red_keys: u8,
    blue_keys: u8,
    green_keys: u8,
    total_swords: u8,
    total_gold_keys: u8,
    total_silver_keys: u8,
    total_red_keys: u8,
    total_blue_keys: u8,
    total_green_keys: u8,
    portal_stones: u8,
    gems: u8,
    treasures: u8,
    total_portal_stones: u8,
    total_gems: u8,
    total_treasures: u8,
    hammers: u8,
    boots: u8,
    compasses: u8,
    castle_puzzle: u8,
    castle_entered: u8,
    green_key: u8,
    bloodmoon_effect: u8,
    bloodmoon_count: u8,
    bloodmoon_orb_hide: u8,
    boss_reached: u8,
    castle_labyrinth_open: u8,
    swamp_secret: u8,
    back_door_lock_1: u8,
    back_door_lock_2: u8,
    victory_road_solved: u8,
    dragon_treasure: u8,
    secret_sockets: u8,
    secret_tokens: u8,
    castle_skip_primed: u8,
    castle_puzzle_solved: u8,
    warp_portal_01: u8,
    warp_portal_02: u8,
    warp_portal_03: u8,
    warp_portal_04: u8,
    warp_portal_05: u8,
    warp_portal_06: u8,
    warp_portal_07: u8,
    warp_portal_08: u8,
    warp_portal_09: u8,
    warp_portal_10: u8,
    ghost_ship_entered: u8,
    hermit_sword_acquired: u8,
    gem_sword: u8,
    gold_sword: u8,
    total_steps: u8,
    witch_cloak: u8,
    lava_charms: u8,
    gold_sword_door: u8,
    spectacles: u8,
    skeleton_key: u8,
    water_ring: u8,
    shield: u8,
    gem_shield: u8,
    stopwatch: u8,
    snake_boss_defeated: u8,
    fairyland_lock_1: u8,
    fairyland_lock_2: u8,
    fairyland_lock_3: u8,
    fairyland_lock_4: u8,
    fairyland_lock_5: u8,
    fairyland_lock_6: u8,
    fairyland_lock_7: u8,
    fairyland_lock_8: u8,
    fairyland_locks: u8,
    gem_heart: u8,
    save_count: u8,
    dragon_killed: u8,
    backup_swords: u8,
    backup_gem_sword: u8,
    backup_gem_shield: u8,
    backup_skeleton_key: u8,
    backup_lava_charm: u8,
    backup_water_ring: u8,
    backup_boots: u8,
    backup_spectacles: u8,
    backup_gold_sword: u8,
    purple_keys: u8,
    teal_keys: u8,
    witch_health: u8,
    backup_hammer: u8,
    gem_boots: u8,
    backup_compass: u8,
    greenfight_lock_1: u8,
    greenfight_lock_2: u8,
    greenfight_lock_3: u8,
    greenfight_lock_4: u8,
    greenfight_lock_5: u8,
    greenfight_lock_6: u8,
    greenfight_lock_7: u8,
    greenfight_lock_8: u8,
    backup_red_key: u8,
    backup_green_key: u8,
    backup_blue_key: u8,
    backup_shield: u8,
    backup_treasures: u8,
    backup_gems: u8,
    backup_portal_stones: u8,
    backup_silver_keys: u8,
    backup_gold_keys: u8,
    backup_t_treasures: u8,
    backup_t_gems: u8,
    backup_t_portal_stones: u8,
    backup_t_silver_keys: u8,
    backup_t_gold_keys: u8,
    total_gold_doors: u8,
    total_silver_doors: u8,
    total_teal_doors: u8,
    total_purple_doors: u8,
    total_red_doors: u8,
    total_blue_doors: u8,
    total_green_doors: u8,
    total_tokens: u8,
    total_teal_keys: u8,
    total_purple_keys: u8,
    witch_phase1: u8,
    witch_phase2: u8,
    witch_phase3: u8,
    bunny_crime_scene: u8,
    backup_gem_boots: u8,
    overkill: u8,
    backdoor_banditry: u8,
    dragonslain: u8,
    witch_hammer: u8,
    witch_water_ring: u8,
    witch_lava_charm: u8,
    witch_skeleton_key: u8,
    witch_compass: u8,
    witch_boots: u8,
    witch_gem_sword_1: u8,
    witch_gem_sword_2: u8,
    witch_gem_sword_3: u8,
    completion_swamp: u8,
    completion_maze: u8,
    completion_boots: u8,
    completion_cloak: u8,
    ngp: u8,
    ngp_tokens: u8,
    red_sword: u8,
    red_shield: u8,
    red_gear_skip: u8,
    ngpp: u8,
    rdragon_killed: u8,
    total_ngp_tokens: u8,
    prevented_night: u8,
    convergence_key: u8,
    possum_coins: u8,
    total_possum_coins: u8,
    backup_t_possum_coins: u8,
    backup_possum_coins: u8,
    broom: u8,
    backup_broom: u8,
    collector_eye: u8,
    backup_collector_eye: u8,
    mirror: u8,
    backup_mirror: u8,
    dragon_egg: u8,
    backup_dragon_egg: u8,
    save_crystal: u8,
    backup_save_crystal: u8,
    carrot: u8,
    backup_carrot: u8,
    green_sword: u8,
    green_shield: u8,
    backup_green_sword: u8,
    backup_green_shield: u8,
    bunny_love: u8,
    bunny_level: u8,
    hero_form_override: u8,
    hero_color_override: u8,
    bunny_color_override: u8,
    evil_bunny_tamed: u8,
    backup_ngp_tokens: u8,
    backup_secret_tokens: u8,
    backup_t_ngp_tokens: u8,
    backup_t_secret_tokens: u8,
    fishing_pole: u8,
    fish: u8,
}

impl From<Vec<u8>> for Inventory {
    fn from(mut v: Vec<u8>) -> Self {
        v.reverse();
        Inventory {
            hearts: v.pop().unwrap(),
            swords: v.pop().unwrap(),
            gold_keys: v.pop().unwrap(),
            silver_keys: v.pop().unwrap(),
            red_keys: v.pop().unwrap(),
            blue_keys: v.pop().unwrap(),
            green_keys: v.pop().unwrap(),
            total_swords: v.pop().unwrap(),
            total_gold_keys: v.pop().unwrap(),
            total_silver_keys: v.pop().unwrap(),
            total_red_keys: v.pop().unwrap(),
            total_blue_keys: v.pop().unwrap(),
            total_green_keys: v.pop().unwrap(),
            portal_stones: v.pop().unwrap(),
            gems: v.pop().unwrap(),
            treasures: v.pop().unwrap(),
            total_portal_stones: v.pop().unwrap(),
            total_gems: v.pop().unwrap(),
            total_treasures: v.pop().unwrap(),
            hammers: v.pop().unwrap(),
            boots: v.pop().unwrap(),
            compasses: v.pop().unwrap(),
            castle_puzzle: v.pop().unwrap(),
            castle_entered: v.pop().unwrap(),
            green_key: v.pop().unwrap(),
            bloodmoon_effect: v.pop().unwrap(),
            bloodmoon_count: v.pop().unwrap(),
            bloodmoon_orb_hide: v.pop().unwrap(),
            boss_reached: v.pop().unwrap(),
            castle_labyrinth_open: v.pop().unwrap(),
            swamp_secret: v.pop().unwrap(),
            back_door_lock_1: v.pop().unwrap(),
            back_door_lock_2: v.pop().unwrap(),
            victory_road_solved: v.pop().unwrap(),
            dragon_treasure: v.pop().unwrap(),
            secret_sockets: v.pop().unwrap(),
            secret_tokens: v.pop().unwrap(),
            castle_skip_primed: v.pop().unwrap(),
            castle_puzzle_solved: v.pop().unwrap(),
            warp_portal_01: v.pop().unwrap(),
            warp_portal_02: v.pop().unwrap(),
            warp_portal_03: v.pop().unwrap(),
            warp_portal_04: v.pop().unwrap(),
            warp_portal_05: v.pop().unwrap(),
            warp_portal_06: v.pop().unwrap(),
            warp_portal_07: v.pop().unwrap(),
            warp_portal_08: v.pop().unwrap(),
            warp_portal_09: v.pop().unwrap(),
            warp_portal_10: v.pop().unwrap(),
            ghost_ship_entered: v.pop().unwrap(),
            hermit_sword_acquired: v.pop().unwrap(),
            gem_sword: v.pop().unwrap(),
            gold_sword: v.pop().unwrap(),
            total_steps: v.pop().unwrap(),
            witch_cloak: v.pop().unwrap(),
            lava_charms: v.pop().unwrap(),
            gold_sword_door: v.pop().unwrap(),
            spectacles: v.pop().unwrap(),
            skeleton_key: v.pop().unwrap(),
            water_ring: v.pop().unwrap(),
            shield: v.pop().unwrap(),
            gem_shield: v.pop().unwrap(),
            stopwatch: v.pop().unwrap(),
            snake_boss_defeated: v.pop().unwrap(),
            fairyland_lock_1: v.pop().unwrap(),
            fairyland_lock_2: v.pop().unwrap(),
            fairyland_lock_3: v.pop().unwrap(),
            fairyland_lock_4: v.pop().unwrap(),
            fairyland_lock_5: v.pop().unwrap(),
            fairyland_lock_6: v.pop().unwrap(),
            fairyland_lock_7: v.pop().unwrap(),
            fairyland_lock_8: v.pop().unwrap(),
            fairyland_locks: v.pop().unwrap(),
            gem_heart: v.pop().unwrap(),
            save_count: v.pop().unwrap(),
            dragon_killed: v.pop().unwrap(),
            backup_swords: v.pop().unwrap(),
            backup_gem_sword: v.pop().unwrap(),
            backup_gem_shield: v.pop().unwrap(),
            backup_skeleton_key: v.pop().unwrap(),
            backup_lava_charm: v.pop().unwrap(),
            backup_water_ring: v.pop().unwrap(),
            backup_boots: v.pop().unwrap(),
            backup_spectacles: v.pop().unwrap(),
            backup_gold_sword: v.pop().unwrap(),
            purple_keys: v.pop().unwrap(),
            teal_keys: v.pop().unwrap(),
            witch_health: v.pop().unwrap(),
            backup_hammer: v.pop().unwrap(),
            gem_boots: v.pop().unwrap(),
            backup_compass: v.pop().unwrap(),
            greenfight_lock_1: v.pop().unwrap(),
            greenfight_lock_2: v.pop().unwrap(),
            greenfight_lock_3: v.pop().unwrap(),
            greenfight_lock_4: v.pop().unwrap(),
            greenfight_lock_5: v.pop().unwrap(),
            greenfight_lock_6: v.pop().unwrap(),
            greenfight_lock_7: v.pop().unwrap(),
            greenfight_lock_8: v.pop().unwrap(),
            backup_red_key: v.pop().unwrap(),
            backup_green_key: v.pop().unwrap(),
            backup_blue_key: v.pop().unwrap(),
            backup_shield: v.pop().unwrap(),
            backup_treasures: v.pop().unwrap(),
            backup_gems: v.pop().unwrap(),
            backup_portal_stones: v.pop().unwrap(),
            backup_silver_keys: v.pop().unwrap(),
            backup_gold_keys: v.pop().unwrap(),
            backup_t_treasures: v.pop().unwrap(),
            backup_t_gems: v.pop().unwrap(),
            backup_t_portal_stones: v.pop().unwrap(),
            backup_t_silver_keys: v.pop().unwrap(),
            backup_t_gold_keys: v.pop().unwrap(),
            total_gold_doors: v.pop().unwrap(),
            total_silver_doors: v.pop().unwrap(),
            total_teal_doors: v.pop().unwrap(),
            total_purple_doors: v.pop().unwrap(),
            total_red_doors: v.pop().unwrap(),
            total_blue_doors: v.pop().unwrap(),
            total_green_doors: v.pop().unwrap(),
            total_tokens: v.pop().unwrap(),
            total_teal_keys: v.pop().unwrap(),
            total_purple_keys: v.pop().unwrap(),
            witch_phase1: v.pop().unwrap(),
            witch_phase2: v.pop().unwrap(),
            witch_phase3: v.pop().unwrap(),
            bunny_crime_scene: v.pop().unwrap(),
            backup_gem_boots: v.pop().unwrap(),
            overkill: v.pop().unwrap(),
            backdoor_banditry: v.pop().unwrap(),
            dragonslain: v.pop().unwrap(),
            witch_hammer: v.pop().unwrap(),
            witch_water_ring: v.pop().unwrap(),
            witch_lava_charm: v.pop().unwrap(),
            witch_skeleton_key: v.pop().unwrap(),
            witch_compass: v.pop().unwrap(),
            witch_boots: v.pop().unwrap(),
            witch_gem_sword_1: v.pop().unwrap(),
            witch_gem_sword_2: v.pop().unwrap(),
            witch_gem_sword_3: v.pop().unwrap(),
            completion_swamp: v.pop().unwrap(),
            completion_maze: v.pop().unwrap(),
            completion_boots: v.pop().unwrap(),
            completion_cloak: v.pop().unwrap(),
            ngp: v.pop().unwrap(),
            ngp_tokens: v.pop().unwrap(),
            red_sword: v.pop().unwrap(),
            red_shield: v.pop().unwrap(),
            red_gear_skip: v.pop().unwrap(),
            ngpp: v.pop().unwrap(),
            rdragon_killed: v.pop().unwrap(),
            total_ngp_tokens: v.pop().unwrap(),
            prevented_night: v.pop().unwrap(),
            convergence_key: v.pop().unwrap(),
            possum_coins: v.pop().unwrap(),
            total_possum_coins: v.pop().unwrap(),
            backup_t_possum_coins: v.pop().unwrap(),
            backup_possum_coins: v.pop().unwrap(),
            broom: v.pop().unwrap(),
            backup_broom: v.pop().unwrap(),
            collector_eye: v.pop().unwrap(),
            backup_collector_eye: v.pop().unwrap(),
            mirror: v.pop().unwrap(),
            backup_mirror: v.pop().unwrap(),
            dragon_egg: v.pop().unwrap(),
            backup_dragon_egg: v.pop().unwrap(),
            save_crystal: v.pop().unwrap(),
            backup_save_crystal: v.pop().unwrap(),
            carrot: v.pop().unwrap(),
            backup_carrot: v.pop().unwrap(),
            green_sword: v.pop().unwrap(),
            green_shield: v.pop().unwrap(),
            backup_green_sword: v.pop().unwrap(),
            backup_green_shield: v.pop().unwrap(),
            bunny_love: v.pop().unwrap(),
            bunny_level: v.pop().unwrap(),
            hero_form_override: v.pop().unwrap(),
            hero_color_override: v.pop().unwrap(),
            bunny_color_override: v.pop().unwrap(),
            evil_bunny_tamed: v.pop().unwrap(),
            backup_ngp_tokens: v.pop().unwrap(),
            backup_secret_tokens: v.pop().unwrap(),
            backup_t_ngp_tokens: v.pop().unwrap(),
            backup_t_secret_tokens: v.pop().unwrap(),
            fishing_pole: v.pop().unwrap(),
            fish: v.pop().unwrap(),
        }
    }
}
